import React from "react";
import "./Display.css";

const Display = () => {
    return (
        <div className='display jumbotron text-center'>
        <h1>Clicky Game!</h1>
        <p>Click on a image to earn points, but don't click on any image more than once! Click on all the characters to win the game.</p>
        </div>
    )
};

export default Display;